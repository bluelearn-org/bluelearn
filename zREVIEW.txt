1- update all endpoints in /edit/guide to /edit/guide/{slug}

2- Create new page reusing existing components


ORIGINAL ISSUE:

Problem
Users should be able to edit a guide after submission

Proposal
Create a new page /edit/guide/{slug}
Include all fields from the GuideDetails component in contribution flow and the content component from the contribution flow.
It should load the guide details into the fields then allow the user to update it.
Implement the save as a draft and submit functionality.

There is no need to create new components so reuse existing components, and ensure styling is consistent and matches with the rest of the platform (see other pages component and reuse classNames if needed).

Area
app (frontend)
